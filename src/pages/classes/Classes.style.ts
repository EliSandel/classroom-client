import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles(() => ({
    classesPage: {
        width: "100%",
        display: "flex",
        flexWrap: "wrap",
        justifyContent: "space-evenly"
    }
})) 