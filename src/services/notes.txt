├── services/          # Service layer for handling business logic
│   ├── userService.js # User-related business logic, might include transformations
│   └── postService.js # Post-related logic

// services/userService.js
import { fetchUser } from '../api/user';

export const getUserDetails = async (id) => {
    const data = await fetchUser(id);
    return {
        id: data.id,
        name: data.full_name, // example transformation
        email: data.email,
    };
};
