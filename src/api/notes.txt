├── api/               # API layer for making network requests
│   ├── client.js      # Axios or Fetch client configuration (optional)
│   ├── user.js        # User-related API calls
│   └── post.js        # Post-related API calls (if applicable)

// api/user.js
import axios from 'axios';

export const fetchUser = async (id) => {
    const response = await axios.get(`/api/users/${id}`);
    return response.data;
};
